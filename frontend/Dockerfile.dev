FROM node:22-alpine

WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install
COPY . .
EXPOSE 5173
# Re-install on start so new deps from mounted package.json land in /app/node_modules volume
CMD ["sh", "-c", "npm install && npm run dev -- --host 0.0.0.0"]
