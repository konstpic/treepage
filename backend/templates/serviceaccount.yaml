{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "treepage-backend.serviceAccountName" . }}
  labels:
    {{- include "treepage-backend.labels" . | nindent 4 }}
{{- end }}
