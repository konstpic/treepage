# Copy to .env (optional — repo includes .env with safe defaults)
COMPOSE_BAKE=false

# Shared secret for backend-server → backend-sync API calls (required in production)
INTERNAL_SERVICE_TOKEN=dev-internal-token-change-in-production

# Git webhook validation (required if using webhooks)
GIT_WEBHOOK_SECRET=dev-webhook-secret
